/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'xkydgfjiofsdqsbozuha.supabase.co',
      },
    ],
  },
};

module.exports = nextConfig;

// bs-simple.com | בועז סעדה - פתרונות יצירתיים
